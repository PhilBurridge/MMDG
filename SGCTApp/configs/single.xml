<?xml version="1.0" ?>
<!-- 
masteraddress & address needs to be set to another ip if this is to be run for real, localhost is only used for testing local.

Use telnet to test local: "telnet localhost 20501", 
then send "value 1" or "value 0"
-->
<Cluster masterAddress="localhost" externalControlPort="20501">
	<Node address="localhost" port="20501">
		<Window fullScreen="false" numberOfSamples = "8">
			<!-- 16:9 aspect ratio -->
			<Size x="640" y="360" />
			<Viewport>
				<Pos x="0.0" y="0.0" />
				<Size x="1.0" y="1.0" />
				<Viewplane>
					<!-- Lower left -->
					<Pos x="-1.778" y="-1.0" z="0.0" />
					<!-- Upper left -->
					<Pos x="-1.778" y="1.0" z="0.0" />
					<!-- Upper right -->
					<Pos x="1.778" y="1.0" z="0.0" />
				</Viewplane>
			</Viewport>
		</Window>
	</Node>
	<User eyeSeparation="0.065">
		<Pos x="0.0" y="0.0" z="4.0" />
	</User>
</Cluster>